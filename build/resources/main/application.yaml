spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:career-tracker-loca2gaming-18ad.g.aivencloud.com}:${DB_PORT:13765}/${DB_NAME:defaultdb}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USER:avnadmin}
    password: ${DB_PASSWORD:AVNS_pQo1WsnuBn7vq6uQGBu}

  jpa:
    hibernate:
      ddl-auto: update

  application:
    name: CareerTracker
